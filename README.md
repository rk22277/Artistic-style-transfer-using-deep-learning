## DATA 255 Deep learning
## Dynamic style transfer for images using deep learning

instructions to run:

clone the repository
install requirements.txt using command, pip install -r requirements.txt
run the command FLASK_APP=main.py flask run
